spring:
  data:
    mongodb:
      uri: mongodb://${embedded.mongodb.host}:${embedded.mongodb.port}/${embedded.mongodb.database}
embedded:
  mongodb:
    install:
      enabled: false
logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: debug
    ru.softmachine.odyssey: debug